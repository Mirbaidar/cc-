#include <stdio.h>
int maize(){
    
}

int main()
{
    int n;
    printf("enter trhe number of columns");
    scanf("%d", &n);

    int m;
    printf("enter rthe number of rows");
    scanf("%d", &m);

    int totalways = maize();
    printf("total number of ways %d",totalways);

    return 0;
}