#include <stdio.h>
int main()
{
    int f, i, v, e, sum;
    printf("enter trhe number");
    scanf("%d%d%d%d", &f, &i, &v, &e);
    sum = f /1 + i / 1 + v / 1 + e / 1;
    printf("sum = %d",sum);
    return (0);
}